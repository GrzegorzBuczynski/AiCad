# Model Schema (Draft)

- schema_version
- name
- units
- parameters
- features (hierarchical dependency by feature IDs)
- metadata

## Notes

- Extrude must reference `profile_id`.
- Rebuild supports full model or branch-from-node.
- AI proposals are merged through local Git workflow only.
